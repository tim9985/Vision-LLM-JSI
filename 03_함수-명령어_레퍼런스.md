# 03_DL-and-GPU — 핵심 함수 · 명령어 레퍼런스

`03_DL-and-GPU.ipynb`에 **실제로 등장한** 함수와 명령어만 라이브러리별로 모은 빠른 조회용 정리.
`빈도`는 노트북 안에서 쓰인 횟수이며, **숫자가 클수록 먼저 외워야 할 것**이다.

> 개념 설명은 `03_DL-and-GPU_정리.md`, CuPy 실습 정답은 `03_CuPy-GPU_정리.md` 참고.

---

## 목차

| # | 분류 | 대표 |
|---|---|---|
| 1 | [Python 내장 함수 · 표준 라이브러리](#1-python-내장-함수--표준-라이브러리) | `print` `range` `next(iter())` `super()` |
| 2 | [NumPy](#2-numpy) | `np.array` `np.sum` `np.zeros` `np.pad` |
| 3 | [CuPy](#3-cupy) | `cp.asarray` `cp.asnumpy` `synchronize` |
| 4 | [Matplotlib](#4-matplotlib) | `plt.subplot` `plt.imshow` `plt.plot` |
| 5 | [OpenCV](#5-opencv) | `cv2.imread` `cv2.cvtColor` `cv2.resize` |
| 6 | [PyTorch — 코어](#6-pytorch--코어-torch) | `torch.device` `torch.no_grad` `torch.flatten` |
| 7 | [PyTorch — Tensor 메서드](#7-pytorch--tensor-메서드) | `.to()` `.item()` `.unsqueeze()` `.permute()` |
| 8 | [PyTorch — nn (Layer · Loss)](#8-pytorch--nn-layer--loss) | `nn.Conv2d` `nn.Linear` `nn.CrossEntropyLoss` |
| 9 | [PyTorch — 학습 4종 세트](#9-pytorch--학습-4종-세트-optimizer) | `zero_grad` `backward` `step` |
| 10 | [torchvision — datasets · transforms](#10-torchvision--datasets--transforms) | `datasets.MNIST` `transforms.Compose` |
| 11 | [모델 저장 · 불러오기](#11-모델-저장--불러오기) | `state_dict` `torch.save` `torch.load` |
| 12 | [쉘 명령어](#12-쉘-명령어) | `pip install --no-deps` `dpkg -l` |
| — | [부록: 헷갈리는 짝 총정리](#부록-헷갈리는-짝-총정리) | |
| — | [부록: 빈출 TOP 20](#부록-빈출-top-20) | |

---

## 1. Python 내장 함수 · 표준 라이브러리

| 빈도 | 함수 | 하는 일 | 노트북 사용 예 |
|---:|---|---|---|
| 210 | `print()` | 출력 | `print("Label:", label)` |
| 134 | `f"..."` | f-string 포맷 | `f"Epoch {epoch+1}/{epochs}"` |
| 26 | `type(x)` | 타입 확인 | `type(y_gpu)` → `<class 'cupy.ndarray'>` |
| 20 | `range(n)` | 0 ~ n-1 반복 | `for epoch in range(epochs):` |
| 20 | `sum(iterable)` | 합계 (제너레이터와 자주 조합) | `sum(p.numel() for p in model.parameters())` |
| 8 | `len(x)` | 길이 | `len(train_dataset)` → 60000 |
| 8 | `min(a, b)` | 최솟값 | `min(feature_maps.shape[0], max_maps)` |
| 7 | `next(iter(loader))` | **첫 Batch만 꺼내기** | `images, labels = next(iter(train_loader))` |
| 5 | `super().__init__()` | 부모 클래스 초기화 | `nn.Module` 상속 시 **필수** |
| 1 | `zip(a, b)` | 두 리스트를 짝지어 순회 | `zip(outputs, gates)` |

### f-string 포맷 스펙 (노트북 전반에서 반복 등장)

```python
f"{train_accuracy * 100:.2f}%"   # 소수점 2자리 → "98.35%"
f"{loss:.4f}"                    # 소수점 4자리 → "0.0521"
f"{epoch:5d}"                    # 정수 5칸 오른쪽 정렬 → "    0"
f"{name:>{len(name)+2}s}"        # 폭을 변수로 지정한 오른쪽 정렬
```

| 스펙 | 의미 |
|---|---|
| `:.2f` | 실수, 소수점 2자리 고정 |
| `:5d` | 정수, 폭 5칸 |
| `:>10s` | 문자열, 폭 10칸 **오른쪽** 정렬 (`<`는 왼쪽, `^`는 가운데) |

### `next(iter(...))` 패턴 — 반드시 이해할 것

```python
images, labels = next(iter(train_loader))
```

| 단계 | 하는 일 |
|---|---|
| `iter(train_loader)` | DataLoader를 **반복자(Iterator)** 로 변환 |
| `next(...)` | 반복자에서 **다음 항목 하나** 를 꺼냄 → 첫 Batch |

> 전체를 다 도는 `for images, labels in train_loader:` 와 달리, **디버깅용으로 Batch 하나만 확인**할 때 쓴다.

### `super().__init__()` — 빼먹으면 모델이 안 돈다

```python
class SimpleCNN(nn.Module):
    def __init__(self):
        super().__init__()          # ← 이 줄이 없으면 파라미터 등록이 안 됨
        self.conv1 = nn.Conv2d(...)
```

> `nn.Module`은 내부에 파라미터·서브모듈을 등록하는 딕셔너리를 만든다. `super().__init__()`을 빼면 그 준비가 안 되어 `self.conv1 = ...` 대입 시점에 에러가 나거나 `model.parameters()`가 비게 된다.

### 표준 라이브러리

| 빈도 | 함수 | 하는 일 | 주의 |
|---:|---|---|---|
| 21 | `time.perf_counter()` | 고해상도 경과 시간 측정 | `time.time()`보다 정밀. **차이(delta)를 구하는 용도로만** 쓸 것 |
| 3 | `random.seed(42)` | Python 난수 고정 | NumPy·PyTorch와 **별개**의 생성기 |

```python
start = time.perf_counter()
...
elapsed = time.perf_counter() - start
```

### 난수 고정 3종 세트 (셀 148)

```python
seed = 42

random.seed(seed)          # Python 표준 random
np.random.seed(seed)       # NumPy
torch.manual_seed(seed)    # PyTorch (CPU)
# GPU까지 쓴다면
torch.cuda.manual_seed_all(seed)
```

> **세 라이브러리가 각자 다른 난수 생성기를 쓰기 때문에 전부 고정해야 한다.**
> 그래도 cuDNN의 알고리즘 자동 선택 때문에 완전히 동일한 결과는 보장되지 않는다 — 노트북 본문도 "결과가 완전히 동일하지 않을 수 있지만 재현성을 높일 수 있다"고 적고 있다.

---

## 2. NumPy

### 배열 생성

| 빈도 | 함수 | 하는 일 | 예 |
|---:|---|---|---|
| 23 | `np.array(list)` | 리스트 → 배열 | `np.array([0.5, 0.5])` |
| 5 | `np.zeros(shape)` | 0으로 채운 배열 | `np.zeros((h, w), dtype=np.float32)` |
| 2 | `np.linspace(a, b, n)` | a~b를 **n등분**한 배열 | `np.linspace(1.0, 0.05, 32)` |
| 1 | `np.arange(n)` | 0 ~ n-1 정수 배열 | `np.arange(10)` |
| 2 | `np.random.rand(h, w)` | 0~1 균등 난수 | `np.random.rand(1000,1000)` |
| 1 | `np.random.default_rng(seed)` | **권장** 난수 생성기 객체 | `rng = np.random.default_rng(42)` |
| 2 | `rng.normal(loc, scale, size)` | 정규분포 난수 | 가중치 초기화 |

> **`np.linspace(a, b, n)` vs `np.arange(a, b, step)`**
> `linspace`는 **개수**를 지정(끝값 포함), `arange`는 **간격**을 지정(끝값 제외). 잔상 효과 가중치는 "32개"가 필요하므로 `linspace`.

### 연산 · 축약

| 빈도 | 함수 | 하는 일 | 주의 |
|---:|---|---|---|
| 10 | `np.sum(x)` | 전체 합 | `axis=` 지정 시 축 방향 합 |
| 5 | `np.mean(x)` | 평균 | `np.mean(error, axis=0)` |
| 2 | `np.max(x)` / `np.min(x)` | **배열 전체의** 최대/최소 **하나** | ReLU에 쓰면 안 됨! |
| 1 | `np.maximum(a, b)` | **두 배열을 원소별 비교**해 큰 값 | ReLU는 **이것** |
| 2 | `np.argmax(x)` | 최댓값의 **인덱스** | |
| 1 | `np.exp(x)` | 지수 함수 | sigmoid 구현 |
| 2 | `np.log(x)` | 자연로그 | BCE 구현 |
| 1 | `np.ceil(x)` | 올림 | 서브플롯 행 개수 계산 |

```python
# ⚠️ 가장 흔한 혼동
def relu(x):
    return np.maximum(0, x)   # ✅ 원소별 max → 배열 반환
    # return np.max(0, x)     # ❌ 전체 최댓값 → 에러/스칼라
```

### 형태 · 변환

| 빈도 | 함수 | 하는 일 | 예 |
|---:|---|---|---|
| 9 | `.astype(dtype)` | 자료형 변환 | `img_gray.astype(np.float32) / 255.` |
| 1 | `np.pad(x, ((t,b),(l,r)), mode, constant_values)` | 가장자리 채우기 | zero padding |
| — | `x.shape` | 배열 크기 튜플 | `height, width = image.shape` |
| — | `x.T` | 전치 | `X.T @ error` |
| — | `x @ y` | 행렬곱 | `X @ w + b` |
| — | `x * y` | **원소별** 곱 | `np.sum(x * w)` |

```python
padded_image = np.pad(
    image,
    ((padding, padding), (padding, padding)),   # ((위,아래), (왼,오))
    mode="constant",
    constant_values=0
)
```

> **`@` vs `*`**
> `@` = 행렬곱(내적), `*` = 원소별 곱. 퍼셉트론에서 `np.sum(x * w)`와 `x @ w`는 결과가 같지만, 배치 연산 `X @ w`(shape `(4,2)@(2,1)`)에서는 **`@`만** 맞다.

### dtype

| 상수 | 의미 | 왜 쓰나 |
|---|---|---|
| `np.float32` | 32비트 실수 | 딥러닝 표준. `float64`의 절반 메모리, GPU에서 훨씬 빠름 |
| `dtype=int` | 정수 | `np.array(x > 0, dtype=int)` — bool을 0/1로 |

---

## 3. CuPy

> 전체 정리는 `03_CuPy-GPU_정리.md` 참고. 여기서는 명령어만.

| 빈도 | 함수 | 하는 일 | 대응 NumPy |
|---:|---|---|---|
| 7 | `cp.cuda.Stream.null.synchronize()` | **GPU 작업 완료 대기** | (없음 — CPU는 동기적) |
| 2 | `cp.asarray(x)` | **CPU → GPU** 복사 | — |
| 1 | `cp.asnumpy(x)` | **GPU → CPU** 복사 | — |
| 2 | `cp.array([...])` | GPU 배열 생성 (항상 복사) | `np.array` |
| 1 | `cp.zeros(shape, dtype)` | 0 배열 | `np.zeros` |
| 1 | `cp.arange(n)` | 정수 배열 | `np.arange` |
| 2 | `cp.random.rand(h, w, dtype=)` | 난수 | `np.random.rand().astype()` |
| 1 | `cp.sum/max/min/mean/argmax` | 축약 | `np.sum` 등 |
| 1 | `cp.cuda.runtime.getDeviceCount()` | GPU 개수 | — |
| 1 | `cp.cuda.runtime.runtimeGetVersion()` | CUDA 런타임 버전 | `12060` = 12.6.0 |
| — | `cp.pad(...)` | 패딩 | `np.pad` — 인자 동일 |
| — | `cp.maximum(0, x)` | ReLU | `np.maximum` |
| — | `.item()` | 0차원 배열 → Python 숫자 | — |

### 필수 3줄

```python
x_gpu  = cp.asarray(x_cpu)              # 올리고
y_gpu  = x_gpu ** 2                     # GPU에서 연산
y_cpu  = cp.asnumpy(y_gpu)              # 내리고
```

### 시간 측정 템플릿

```python
cp.cuda.Stream.null.synchronize()    # 시작 전
start = time.perf_counter()
result = gpu_function(x_gpu)
cp.cuda.Stream.null.synchronize()    # 종료 전
elapsed = time.perf_counter() - start
```

> `cp.asarray` vs `cp.array` — `asarray`는 이미 CuPy 배열이면 **복사하지 않는다.** 전송용으로는 `asarray`.

---

## 4. Matplotlib

| 빈도 | 함수 | 하는 일 | 자주 쓰는 인자 |
|---:|---|---|---|
| 53 | `plt.title(s)` | 제목 | `color="red"`, `fontsize=9` |
| 51 | `plt.imshow(img)` | 이미지 출력 | `cmap="gray"`, `vmin=`, `vmax=` |
| 42 | `plt.subplot(r, c, i)` | 격자 배치 (**i는 1부터**) | |
| 26 | `plt.figure(figsize=(w,h))` | 새 그림 (인치 단위) | |
| 26 | `plt.axis("off")` | 축 숨기기 | |
| 15 | `plt.plot(x, y, ...)` | 선 그래프 | `marker="o"`, `label="..."` |
| 9 | `plt.grid()` | 격자 표시 | |
| 9 | `plt.show()` | 화면에 출력 | |
| 6 | `plt.xlabel/ylabel(s)` | 축 이름 | |
| 6 | `plt.gcf()` | 현재 figure 얻기 | `for ax in plt.gcf().axes:` |
| 5 | `plt.legend()` | 범례 (`label=` 필요) | |
| 3 | `plt.tight_layout()` | 겹침 자동 정리 | |
| 3 | `plt.ylim(a, b)` | y축 범위 고정 | |
| 1 | `plt.suptitle(s)` | 전체 제목 | |

### 자주 쓰는 관용구

```python
# 여러 장을 한 번에 (셀 163)
plt.figure(figsize=(10,8))
for index in range(20):
    image, label = train_dataset[index]
    plt.subplot(4, 5, index + 1)                    # ← +1 필수 (1부터 시작)
    plt.imshow(image.squeeze(0), cmap="gray")
    plt.title(f"Label: {label}")
    plt.axis("off")

# 모든 subplot의 축을 한 번에 끄기 (셀 125)
for ax in plt.gcf().axes:
    ax.axis("off")

# 학습 곡선 (셀 465)
plt.plot(epoch_axis, train_loss_history, marker="o", label="Train loss")
plt.plot(epoch_axis, test_loss_history,  marker="o", label="Test loss")
plt.xlabel("Epoch"); plt.ylabel("Loss"); plt.grid(); plt.legend(); plt.show()
```

| 함정 | 설명 |
|---|---|
| `plt.subplot(4, 5, index)` | 인덱스는 **1부터**. `index + 1`을 빼먹으면 `index=0`에서 에러 |
| `plt.legend()`만 호출 | `plt.plot(..., label="...")`로 이름을 준 것만 범례에 나온다 |
| 흑백 이미지가 컬러로 나옴 | `cmap="gray"` 누락 |
| `imshow` 경고 "Clipping input data..." | 값이 `[0,1]` 밖. 정규화 이미지는 **역정규화 후** 출력 |
| CuPy/GPU 텐서를 그대로 전달 | `cp.asnumpy()` 또는 `.cpu()` 필요 |

---

## 5. OpenCV

| 빈도 | 함수 | 하는 일 | 예 |
|---:|---|---|---|
| 2 | `cv2.imread(path, flag)` | 이미지 읽기 (**BGR** 순서) | `cv2.imread(p, cv2.IMREAD_GRAYSCALE)` |
| 2 | `cv2.cvtColor(img, code)` | 색 공간 변환 | `cv2.COLOR_BGR2RGB` / `cv2.COLOR_BGR2GRAY` |
| 1 | `cv2.resize(img, (w,h), interpolation)` | 크기 변경 | `interpolation=cv2.INTER_AREA` |

```python
img_bgr  = cv2.imread("src/images/seagull.jpg")            # BGR 3채널
img_rgb  = cv2.cvtColor(img_bgr, cv2.COLOR_BGR2RGB)        # matplotlib용
img_gray = cv2.cvtColor(img_bgr, cv2.COLOR_BGR2GRAY)       # 2차원

# 한 번에 흑백으로 읽기
img_gray = cv2.imread(path, cv2.IMREAD_GRAYSCALE)

img_gray = cv2.resize(img_gray, (1280, 720), interpolation=cv2.INTER_AREA)
```

| 함정 | 설명 |
|---|---|
| **BGR ↔ RGB** | `cv2.imread`는 **BGR** 순서. `plt.imshow`에 그대로 넣으면 **색이 뒤집힌다** → `COLOR_BGR2RGB` 필요 |
| **`(width, height)` 순서** | `cv2.resize`의 크기 인자는 `(w, h)`. NumPy `shape`의 `(h, w)`와 **반대** |
| 파일이 없을 때 | 예외가 아니라 **`None`을 반환**한다. `if img is None: raise ...` 로 직접 확인해야 함 |
| `INTER_AREA` | **축소**에 최적. 확대에는 `INTER_LINEAR`/`INTER_CUBIC` |

---

## 6. PyTorch — 코어 (`torch`)

| 빈도 | 함수 | 하는 일 | 예 |
|---:|---|---|---|
| 10 | `torch.no_grad()` | Gradient 계산 **비활성화** (with문) | 평가·추론 시 필수 |
| 6 | `torch.device(s)` | 연산 장치 객체 | `torch.device("cuda")` |
| 5 | `torch.tensor(data)` | 데이터 → Tensor | `torch.tensor(CIFAR10_MEAN)` |
| 5 | `torch.softmax(x, dim=1)` | 확률 분포로 변환 | 추론 시에만 |
| 4 | `torch.flatten(x, start_dim=1)` | 평탄화 | `[B,16,7,7] → [B,784]` |
| 4 | `torch.manual_seed(n)` | 난수 고정 | |
| 4 | `torch.cuda.is_available()` | CUDA 사용 가능 여부 | |
| 2 | `torch.cuda.synchronize()` | GPU 작업 완료 대기 | 시간 측정 |
| 2 | `torch.save(obj, path)` | 저장 | |
| 2 | `torch.load(path, ...)` | 불러오기 | |
| 1 | `torch.randn(shape)` | 표준정규 난수 Tensor | 더미 입력 |
| 1 | `torch.zeros(shape, device=)` | 0 Tensor | GPU warm-up |

### Device 지정 — 실무 표준

```python
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
```

> GPU가 없는 환경에서도 그대로 돈다. `torch.device("cuda")` 하드코딩은 CPU 머신에서 바로 터진다.

### `torch.flatten(x, start_dim=1)` — `start_dim`이 핵심

```python
x = torch.zeros(64, 16, 7, 7)
torch.flatten(x, start_dim=1).shape   # [64, 784]  ← Batch 차원은 보존
torch.flatten(x).shape                # [50176]    ← 전부 뭉개짐 (틀림)
```

> **`start_dim=1`을 빼면 Batch까지 합쳐져 모델이 망가진다.** 0번 축은 항상 Batch이므로 건드리지 않는다.

### `torch.softmax(logits, dim=1)`

```python
probabilities   = torch.softmax(logits, dim=1)          # [B, 10] → 클래스별 확률
predicted_label = probabilities.argmax(dim=1).item()
```

| 인자 | 이유 |
|---|---|
| `dim=1` | logits shape이 `[Batch, 10]`이므로 **클래스 축**. `dim=0`은 배치 축이라 완전히 틀린 결과 |

> ⚠️ **학습 시에는 절대 쓰지 않는다.** `nn.CrossEntropyLoss`에 이미 들어 있어 이중 적용이 된다. **추론에서 확률을 보고 싶을 때만.**

### `torch.no_grad()` vs `model.eval()` — 다른 것

```python
model.eval()                    # Dropout/BatchNorm의 동작 방식 변경
with torch.no_grad():           # Gradient 그래프 생성 차단 (메모리·속도)
    logits = model(images)
```

> 평가할 때는 **둘 다** 필요하다. 하나만 하면 조용히 틀린 결과가 나온다.

---

## 7. PyTorch — Tensor 메서드

| 빈도 | 메서드 | 하는 일 | 주의 |
|---:|---|---|---|
| 31 | `.item()` | **원소 1개** Tensor → Python 숫자 | 원소가 2개 이상이면 에러 |
| 25 | `.to(device)` | 장치 이동 | **텐서는 반환값을 재대입**해야 함 |
| 12 | `.argmax(dim=)` | 최댓값 인덱스 | `dim=1` = 클래스 축 |
| 9 | `.squeeze(0)` | 크기 1인 차원 **제거** | `[1,28,28] → [28,28]` |
| 6 | `.unsqueeze(0)` | 차원 **추가** | `[28,28] → [1,28,28]` |
| 5 | `.cpu()` | GPU → CPU | NumPy/matplotlib 전달 전 |
| 5 | `.permute(1,2,0)` | 축 순서 변경 | `(C,H,W) → (H,W,C)` |
| 4 | `.view(3,1,1)` | 형태 변경 | 브로드캐스팅용 |
| 4 | `.clamp(0,1)` | 값 범위 제한 | 시각화 전 |
| 4 | `.numel()` | 원소 개수 | 파라미터 수 세기 |
| 4 | `.size(0)` | 특정 축 크기 | `images.size(0)` = batch 크기 |
| 2 | `.detach()` | 자동 미분 그래프에서 분리 | 시각화 전 |
| — | `.shape` | 형태 | |
| — | `.device` | 현재 장치 확인 | 디버깅에 유용 |

### `.to(device)`의 함정

| 대상 | 동작 |
|---|---|
| **모델** | **제자리(in-place)** 이동 — `model.to(device)`만 써도 됨 |
| **텐서** | **새 텐서 반환** — `images.to(device)`만 쓰면 **아무 일도 안 일어남** |

```python
model = model.to(device)      # 관례상 재대입
images = images.to(device)    # ← 재대입 필수!
labels = labels.to(device)
```

> 참고: 이미 그 장치에 있는 텐서에 `.to()`를 부르면 복사 없이 **자기 자신을 그대로 반환**한다
> (`t.to("cpu") is t` → `True`). 그래서 CPU에서 테스트할 때는 재대입을 빼먹어도 멀쩡히 돌다가,
> GPU로 옮기는 순간 `Expected all tensors to be on the same device` 에러가 터진다.
> **장치와 무관하게 항상 재대입하는 습관을 들일 것.**

### `.squeeze()` ↔ `.unsqueeze()`

```python
# 모델에 넣기 전: Batch 차원 추가
input_batch = image.unsqueeze(0)      # [1,28,28] → [1,1,28,28]

# 그림 그리기 전: 채널 차원 제거
plt.imshow(image.squeeze(0), cmap="gray")   # [1,28,28] → [28,28]
```

### 시각화 3단 콤보

```python
feature_maps = feature_tensor[0].detach().cpu()
```

| 메서드 | 왜 |
|---|---|
| `[0]` | Batch에서 첫 장만 |
| `.detach()` | gradient 추적 불필요 |
| `.cpu()` | matplotlib는 GPU 텐서를 못 읽음 |

### 역정규화 관용구 (CIFAR-10, 셀 427)

```python
mean = torch.tensor(CIFAR10_MEAN).view(3, 1, 1)   # (3,) → (3,1,1) 브로드캐스팅용
std  = torch.tensor(CIFAR10_STD).view(3, 1, 1)

image_denormalized = image * std + mean            # 정규화의 역연산
image_rgb = image_denormalized.permute(1, 2, 0).clamp(0, 1)   # (C,H,W)→(H,W,C)
plt.imshow(image_rgb)
```

---

## 8. PyTorch — nn (Layer · Loss)

### Layer

| 빈도 | 클래스 | 주요 인자 | 출력 크기 변화 |
|---:|---|---|---|
| 8 | `nn.Conv2d` | `in_channels, out_channels, kernel_size, stride, padding, bias` | `k=3,p=1,s=1`이면 **유지** |
| 7 | `nn.Linear` | `in_features, out_features` | 1차원 크기 변경 |
| 5 | `nn.Module` | (상속용 기반 클래스) | — |
| 4 | `nn.MaxPool2d` | `kernel_size, stride` | `k=2,s=2`면 **절반** |
| 3 | `nn.Sequential` | `*layers` | 순서대로 실행 |
| 2 | `nn.BatchNorm2d` | `num_features` (= 채널 수) | 유지 |
| 2 | `nn.ReLU` | — | 유지 |
| 1 | `nn.AdaptiveAvgPool2d` | `output_size=(1,1)` | **공간 크기를 1×1로** |
| 1 | `nn.Dropout` | `p=0.3` | 유지 |
| 9 | `F.relu(x)` | (함수형 ReLU) | 유지 |

```python
nn.Conv2d(in_channels=1, out_channels=8, kernel_size=3, stride=1, padding=1)
nn.MaxPool2d(kernel_size=2, stride=2)
nn.Linear(16 * 7 * 7, 64)
nn.BatchNorm2d(out_channels)
nn.AdaptiveAvgPool2d(output_size=(1, 1))
nn.Dropout(p=0.3)
```

### `nn.ReLU` vs `F.relu` — 같은 연산, 다른 사용처

| | 형태 | 언제 |
|---|---|---|
| `nn.ReLU()` | **레이어 객체** | `nn.Sequential` 안에 넣을 때 |
| `F.relu(x)` | **함수** | `forward()` 안에서 직접 호출할 때 |

> 학습 파라미터가 없는 연산이라 둘 중 아무거나 써도 결과는 같다. 노트북은 `SimpleCNN.forward()`에서 `F.relu`, `ConvBlock`의 `Sequential`에서 `nn.ReLU()`를 쓴다.

### `bias=False`를 쓰는 경우

```python
nn.Conv2d(..., bias=False),
nn.BatchNorm2d(out_channels),
```

> **`Conv + BatchNorm` 조합에서는 항상 `bias=False`.** BatchNorm이 평균을 빼는 순간 Conv의 편향이 완전히 상쇄되어 사라지므로, 아무 효과 없는 파라미터를 두지 않는다.

### Loss

| 빈도 | 클래스 | 용도 | 입력 |
|---:|---|---|---|
| 3 | `nn.CrossEntropyLoss()` | **다중 클래스 분류** | **raw logits** (Softmax 금지) |

```python
criterion = nn.CrossEntropyLoss()
loss = criterion(logits, labels)   # logits: [B,10] float, labels: [B] int
```

| 항목 | 값 |
|---|---|
| `logits` | `[Batch, num_classes]` — Softmax **거치지 않은** 원시 출력 |
| `labels` | `[Batch]` — **정수 클래스 인덱스** (원-핫 아님) |
| 반환 | batch **평균** loss (스칼라 Tensor) |

### 모델 관련 메서드

| 빈도 | 메서드 | 하는 일 |
|---:|---|---|
| 9 | `model.eval()` | **평가 모드** — Dropout 끄고, BatchNorm은 이동평균 사용 |
| 2 | `model.train()` | **학습 모드** — Dropout 켜고, BatchNorm은 현재 batch 통계 사용 |
| 7 | `model.parameters()` | 학습 파라미터 이터레이터 |
| 3 | `model.state_dict()` | 가중치 딕셔너리 |
| 4 | `model.to(device)` | 장치 이동 |

```python
# 파라미터 수 세기 (셀 185)
parameter_count = sum(p.numel() for p in model.parameters())
trainable_parameter_count = sum(p.numel() for p in model.parameters() if p.requires_grad)
```

---

## 9. PyTorch — 학습 4종 세트 (Optimizer)

| 함수 | 하는 일 | 빼먹으면 |
|---|---|---|
| `optimizer.zero_grad()` | 이전 Batch의 Gradient 제거 | Gradient **누적** → 발산 |
| `loss.backward()` | 역전파 → 각 파라미터의 `.grad` 채움 | Gradient가 없어 **학습 안 됨** |
| `optimizer.step()` | `.grad`로 파라미터 수정 | Gradient는 있는데 **파라미터가 안 바뀜** |
| `model.train()` / `.eval()` | 모드 전환 | Dropout·BatchNorm 오동작 |

### 표준 학습 루프 (순서 중요)

```python
model.train()
for images, labels in train_loader:
    images = images.to(device)
    labels = labels.to(device)

    optimizer.zero_grad()          # ①
    logits = model(images)         # ②
    loss = criterion(logits, labels)
    loss.backward()                # ③
    optimizer.step()               # ④
```

### 표준 평가 루프

```python
model.eval()
with torch.no_grad():
    for images, labels in test_loader:
        images, labels = images.to(device), labels.to(device)
        logits = model(images)
        loss = criterion(logits, labels)
```

### Optimizer

| 빈도 | 생성자 | 특징 |
|---:|---|---|
| 2 | `torch.optim.Adam(model.parameters(), lr=0.001)` | 범용 기본값 |
| 1 | `torch.optim.AdamW(model.parameters(), lr=0.001)` | weight decay를 올바르게 적용. **큰 모델에 권장** |

> 첫 인자는 반드시 `model.parameters()` — "무엇을 수정할지"를 알려 주는 것이다.

### 정확도 집계 관용구

```python
batch_size = images.size(0)

total_loss    += loss.item() * batch_size              # 평균 × 크기 = 합
predictions    = logits.argmax(dim=1)
correct_count += (predictions == labels).sum().item()
sample_count  += batch_size

average_loss = total_loss / sample_count
accuracy     = correct_count / sample_count
```

> `loss.item() * batch_size` — `criterion`이 주는 값은 **batch 평균**이다. 마지막 batch 크기가 다를 수 있으므로 "평균의 평균"을 내면 틀린다. 합으로 되돌린 뒤 총 샘플 수로 나눈다.

---

## 10. torchvision — datasets · transforms

### Dataset

| 빈도 | 함수 | 인자 |
|---:|---|---|
| 6 | `datasets.MNIST(...)` | `root, train, transform, download` |
| 2 | `datasets.CIFAR10(...)` | 동일 |

```python
train_dataset = datasets.MNIST(
    root="src/datasets",   # 저장/로드 경로
    train=True,            # True=학습용, False=테스트용
    transform=transform,   # 전처리
    download=True,         # 없으면 자동 다운로드
)
```

| 데이터셋 | 학습 / 테스트 | 이미지 |
|---|---|---|
| MNIST | 60,000 / 10,000 | `1×28×28` 흑백 |
| CIFAR-10 | 50,000 / 10,000 | `3×32×32` RGB |

### DataLoader

```python
train_loader = DataLoader(
    train_dataset,
    batch_size=64,
    shuffle=True,        # 학습은 True
    num_workers=0,       # 로딩 프로세스 수
    pin_memory=True,     # GPU 전송 가속 (GPU 사용 시)
)
```

| 인자 | 학습 | 테스트 |
|---|---|---|
| `shuffle` | `True` (편향 방지) | `False` |
| `batch_size` | 32~64 | 128~1000 (역전파가 없어 여유) |
| `pin_memory` | GPU면 `True` | GPU면 `True` |

### Transforms

| 빈도 | 함수 | 하는 일 |
|---:|---|---|
| 5 | `transforms.ToTensor()` | `(H,W,C)`→`(C,H,W)` + `0~255`→`0.0~1.0` |
| 3 | `transforms.Normalize(mean, std)` | 채널별 `(x-mean)/std` |
| 2 | `transforms.Compose([...])` | 여러 변환을 순서대로 묶음 |
| 2 | `transforms.RandomCrop(size, padding)` | 무작위 이동 효과 (증강) |
| 2 | `transforms.RandomHorizontalFlip(p)` | 확률적 좌우 반전 (증강) |

```python
train_transform = transforms.Compose([
    transforms.RandomCrop(size=32, padding=4),      # ① PIL 이미지에 동작
    transforms.RandomHorizontalFlip(p=0.5),         # ② PIL 이미지에 동작
    transforms.ToTensor(),                          # ③ Tensor로 변환
    transforms.Normalize(mean=CIFAR10_MEAN, std=CIFAR10_STD),  # ④ Tensor에 동작
])
```

> **`Compose` 순서 규칙**: 증강(`RandomCrop`, `RandomHorizontalFlip`)은 `ToTensor()` **앞**, `Normalize`는 `ToTensor()` **뒤**.
>
> **증강은 학습 데이터만, 정규화는 학습·테스트 모두.**

---

## 11. 모델 저장 · 불러오기

### 방식 A — 가중치만 (MNIST, 셀 252)

```python
# 저장
torch.save(model.state_dict(), "src/models/MNIST/MNIST_CNN.pth")

# 불러오기
loaded_model = SimpleCNN()                      # ← 구조는 코드로 다시 만들어야 함
state_dict = torch.load(path, map_location="cpu", weights_only=True)
loaded_model.load_state_dict(state_dict)
loaded_model.to(device)
loaded_model.eval()                             # ← 필수
```

### 방식 B — 체크포인트 딕셔너리 (CIFAR-10, 셀 471)

```python
save_data = {
    "model_name": "CIFAR10CNN",
    "model_state_dict": model.state_dict(),
    "optimizer_state_dict": optimizer.state_dict(),   # 학습 재개용
    "test_accuracy": test_accuracy_history[-1],
    "epochs": EPOCHS,
    "class_names": CIFAR10_CLASSES,
    "mean": CIFAR10_MEAN,
    "std": CIFAR10_STD,
}
torch.save(save_data, path)

# 불러오기 — 딕셔너리에서 한 번 더 꺼낸다
state_dict = torch.load(path, map_location="cpu", weights_only=True)
loaded_model.load_state_dict(state_dict["model_state_dict"])
```

| 인자 | 의미 |
|---|---|
| `map_location="cpu"` | GPU에서 저장한 모델을 CPU 환경에서도 열 수 있게 함 |
| `weights_only=True` | 텐서·기본 자료형만 읽어 **임의 코드 실행 차단**. 보안상 권장 |

> **`state_dict`는 구조가 아니라 가중치만 담는다.** 불러오기 전에 반드시 같은 클래스를 정의해 인스턴스를 만들어야 한다.
> 전처리 통계(`mean`/`std`)를 함께 저장하는 이유: **추론 때 학습과 다른 전처리를 쓰면 성능이 무너진다.**

---

## 12. 쉘 명령어

### 환경 확인

```bash
dpkg -l | grep nvidia-jetpack        # JetPack 버전 확인
cat /usr/local/cuda/version.json     # CUDA 버전 확인
```

| 명령 | 의미 |
|---|---|
| `dpkg -l` | 설치된 데비안 패키지 목록 |
| `\| grep X` | 출력에서 `X`가 포함된 줄만 필터 |
| `cat` | 파일 내용 출력 |

### PyTorch 설치 (JetPack 6.2.1 + CUDA 12.6)

```bash
pip install torch==2.8.0 torchvision --index-url https://pypi.jetson-ai-lab.io/jp6/cu126 --no-deps
pip uninstall torchvision
pip install torchvision==0.23.0 --index-url https://pypi.jetson-ai-lab.io/jp6/cu126 --no-deps
pip install "sympy>=1.13.3" --no-deps
```

### 모델 시각화 도구

```bash
pip install Pillow==10.4.0 --no-deps
pip install aggdraw==1.3.19 --no-deps
pip install visualtorch==1.4.1 --no-deps
```

### CuPy 설치

```bash
python -m pip download --no-deps --only-binary=:all: "cupy-cuda12x==12.3.0" -d ~/Downloads
python -m pip install --no-deps "fastrlock==0.8.3"
python -m pip install --no-deps --only-binary=:all: ~/Downloads/cupy_cuda12x-12.3.0-*.whl
```

### pip 옵션 정리

| 옵션 | 의미 | 왜 쓰나 |
|---|---|---|
| `--no-deps` | 의존성을 **설치하지 않음** | pip가 NumPy 등을 멋대로 다른 버전으로 덮어써 JetPack 환경이 깨지는 것을 방지 |
| `--index-url URL` | 기본 PyPI 대신 지정한 저장소 사용 | Jetson용 빌드는 `pypi.jetson-ai-lab.io`에만 있음 |
| `--only-binary=:all:` | 소스 빌드 금지, **휠(.whl)만** | Jetson에서 소스 컴파일은 매우 오래 걸리고 자주 실패 |
| `download ... -d DIR` | 설치하지 않고 **다운로드만** | 휠을 받아 두고 나중에 오프라인 설치 |
| `python -m pip` | 현재 파이썬에 연결된 pip 사용 | 여러 파이썬이 깔린 환경에서 pip가 엉뚱한 곳에 설치되는 것 방지 |
| `pkg==1.2.3` | 정확한 버전 고정 | 재현 가능한 환경 |

> `--no-deps`를 쓰면 의존성이 자동으로 안 깔리므로 **`fastrlock`처럼 필요한 것은 직접 설치**해야 한다.

### 패키지 이름 읽는 법

```text
cupy_cuda12x-12.3.0-*.whl
     └─┬──┘  └─┬──┘
   CUDA 12.x용  CuPy 버전
```

> CUDA 버전이 다르면 **다른 패키지**를 받아야 한다 (`cupy-cuda11x` 등).

---

## 부록: 헷갈리는 짝 총정리

| A | B | 차이 |
|---|---|---|
| `np.max(x)` | `np.maximum(a, b)` | 전체 최댓값 **하나** / **원소별** 비교. **ReLU는 maximum** |
| `x * y` | `x @ y` | 원소별 곱 / 행렬곱 |
| `np.linspace(a,b,n)` | `np.arange(a,b,s)` | **개수** 지정(끝 포함) / **간격** 지정(끝 제외) |
| `cp.asarray(x)` | `cp.array(x)` | 이미 CuPy면 복사 안 함 / 항상 복사 |
| `cp.asarray(x)` | `cp.asnumpy(x)` | CPU **→** GPU / GPU **→** CPU |
| `.squeeze(0)` | `.unsqueeze(0)` | 차원 **제거** / **추가** |
| `nn.ReLU()` | `F.relu(x)` | 레이어 객체(`Sequential`용) / 함수(`forward`용) |
| `model.train()` | `model.eval()` | Dropout 켬·BN 갱신 / Dropout 끔·BN 이동평균 |
| `model.eval()` | `torch.no_grad()` | **동작 방식** 변경 / **Gradient 그래프** 차단. 평가엔 **둘 다** |
| `.to(device)` (모델) | `.to(device)` (텐서) | 제자리 이동 / **새 텐서 반환 → 재대입 필수** |
| `torch.flatten(x)` | `torch.flatten(x, start_dim=1)` | 전부 뭉갬 / **Batch 보존**. 항상 후자 |
| `dim=0` | `dim=1` | 배치 축 / **클래스 축**. softmax·argmax는 `dim=1` |
| `logits` | `probabilities` | raw 출력(Loss에 입력) / softmax 결과(추론 표시용) |
| `cv2` BGR | `plt` RGB | `cvtColor(..., COLOR_BGR2RGB)` 필요 |
| `cv2.resize(img,(w,h))` | `img.shape → (h,w)` | **순서가 반대** |
| 증강(Augmentation) | 정규화(Normalization) | 학습 데이터만 / **학습·테스트 모두** |
| `cp...synchronize()` | `torch.cuda.synchronize()` | CuPy용 / PyTorch용 |

---

## 부록: 빈출 TOP 20

이것만 외워도 노트북의 대부분을 읽을 수 있다.

| 순위 | 함수 | 빈도 | 한 줄 요약 |
|---:|---|---:|---|
| 1 | `print()` / f-string | 210 | 출력 |
| 2 | `plt.title()` | 53 | 그래프 제목 |
| 3 | `plt.imshow()` | 51 | 이미지 출력 (`cmap="gray"`) |
| 4 | `plt.subplot(r,c,i)` | 42 | 격자 배치 (**i는 1부터**) |
| 5 | `.item()` | 31 | Tensor → Python 숫자 |
| 6 | `plt.figure(figsize=)` | 26 | 새 그림 |
| 7 | `.to(device)` | 25 | 장치 이동 (**텐서는 재대입**) |
| 8 | `np.array()` | 23 | 배열 생성 |
| 9 | `time.perf_counter()` | 21 | 시간 측정 |
| 10 | `np.sum()` / `sum()` | 20 | 합계 |
| 11 | `range()` | 20 | 반복 |
| 12 | `plt.plot(x,y,marker=,label=)` | 15 | 선 그래프 |
| 13 | `.argmax(dim=1)` | 12 | 예측 클래스 |
| 14 | `model.eval()` | 12 | 평가 모드 |
| 15 | `torch.no_grad()` | 10 | Gradient 차단 |
| 16 | `F.relu()` | 9 | 활성화 함수 |
| 17 | `.squeeze(0)` | 9 | 차원 제거 (시각화) |
| 18 | `.astype()` | 9 | dtype 변환 |
| 19 | `cp...synchronize()` | 7 | GPU 대기 |
| 20 | `next(iter(loader))` | 7 | 첫 Batch 확인 |

---

*원본: `03_DL-and-GPU.ipynb` — Physical AI의 Vision-LLM 융합 시청각 멀티모달 시스템 (김규래)*
*개념 정리: `03_DL-and-GPU_정리.md` · CuPy 실습 정답: `03_CuPy-GPU_정리.md`*
